<!-- ═══════════════════════════════════════════════════════════ --><!-- 📅 COMPONENTE FECHA - FECHA DEL EVENTO DE BODA             --><!-- ═══════════════════════════════════════════════════════════ -->
<section class="py-2 md:py-10">
  <div class="max-w-2xl mx-auto text-center relative">
    <!-- 🍃 HOJA IZQUIERDA - SIEMPRE PEGADA AL BORDE IZQUIERDO -->
    <div
      class="absolute left-0 top-1/2 -translate-y-1/3 w-16 md:w-20 pointer-events-none z-10"
      id="hoja-fecha-izq"
    >
      <img
        src="/imagenes/florlateral.png"
        alt="Hoja izquierda"
        class="w-full h-auto object-contain"
      />
    </div>

    <!-- 🍃 HOJA DERECHA - SIEMPRE PEGADA AL BORDE DERECHO -->
    <div
      class="absolute right-0 top-1/2 -translate-y-1/3 w-16 md:w-20 pointer-events-none z-10"
      id="hoja-fecha-der"
    >
      <img
        src="/imagenes/florlateral.png"
        alt="Hoja derecha"
        class="w-full h-auto object-contain -scale-x-100"
      />
    </div>

    <!-- 🕊️ PALOMAS CON ANILLOS - VUELAN DESDE LOS COSTADOS AL CENTRO -->
    <div
      class="flex justify-center items-center mb-4 md:mb-20 relative h-16"
      id="palomas-container"
    >
      <!-- Paloma izquierda -->
      <div
        class="absolute left-1/2"
        id="paloma-izq"
        style="transform: translateX(-100%);"
      >
        <img
          src="/imagenes/paloma.png"
          alt="Paloma izquierda"
          class="w-16 md:w-32 lg:w-36 h-auto"
        />
      </div>

      <!-- Paloma derecha (volteada) -->
      <div
        class="absolute right-1/2"
        id="paloma-der"
        style="transform: translateX(100%) scaleX(-1);"
      >
        <img
          src="/imagenes/paloma.png"
          alt="Paloma derecha"
          class="w-16 md:w-32 lg:w-36 h-auto"
        />
      </div>
    </div>

    <!-- 💍 Texto "Acompáñanos este día tan especial" -->
    <div class="mb-6" id="texto-invitacion-container">
      <div class="flex items-center justify-center px-4">
        <!-- 📝 TEXTO CENTRAL -->
        <div id="texto-invitacion" class="flex-shrink-0">
          <p
            class="font-great-vibes text-3xl md:text-4xl lg:text-5xl xl:text-5xl text-[#9C7A2E] leading-[1.2] text-center"
          >
            Acompáñanos este día<br />tan especial
          </p>
        </div>
      </div>
    </div>

    <!-- Año 2026 ARRIBA -->
    <div class="mb-4 flex justify-center" id="anio-evento">
      <p
        class="font-charm text-3xl md:text-4xl lg:text-4xl xl:text-4xl text-[#0F3D2E] font-bold"
      >
        2026
      </p>
    </div>

    <!-- 📅 Fecha con líneas arriba y abajo -->
    <div class="relative max-w-md mx-auto" id="fecha-evento">
      <div class="flex items-center justify-center gap-4 md:gap-6">
        <!-- SÁBADO con líneas arriba y abajo -->
        <div class="flex flex-col items-center w-24 md:w-28">
          <div class="w-full h-px bg-[#9C7A2E] mb-2"></div>
          <span
            class="font-charm text-lg md:text-xl lg:text-3xl xl:text-4xl text-[#0F3D2E] uppercase tracking-wider font-bold text-center"
          >
            SÁBADO
          </span>
          <div class="w-full h-px bg-[#9C7A2E] mt-2"></div>
        </div>

        <!-- Número 31 - CENTRADO -->
        <div
          class="flex items-center justify-center min-w-[80px] md:min-w-[100px]"
        >
          <span
            class="font-charm text-7xl md:text-8xl lg:text-8xl xl:text-8xl text-[#9C7A2E] font-bold"
          >
            31
          </span>
        </div>

        <!-- ENERO con líneas arriba y abajo -->
        <div class="flex flex-col items-center w-24 md:w-28">
          <div class="w-full h-px bg-[#9C7A2E] mb-2"></div>
          <span
            class="font-charm text-lg md:text-xl lg:text-3xl xl:text-4xl text-[#0F3D2E] uppercase tracking-wider font-bold text-center"
          >
            ENERO
          </span>
          <div class="w-full h-px bg-[#9C7A2E] mt-2"></div>
        </div>
      </div>
    </div>

    <!-- HORA DEBAJO -->
    <div class="mt-6 flex justify-center" id="hora-evento">
      <p
        class="font-charm text-2xl md:text-3xl lg:text-4xl xl:text-4xl text-[#0F3D2E] font-bold"
      >
        17:00 Hrs.
      </p>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════════════ -->
<!-- 🎬 ANIMACIONES CON GSAP - SEPARACIÓN RESPONSIVE             -->
<!-- ═══════════════════════════════════════════════════════════ -->
<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  if (typeof window !== "undefined") {
    document.addEventListener("DOMContentLoaded", () => {
      console.log("📅 Iniciando animaciones de fecha...");

      // ⚙️ CONFIGURACIÓN DE SEPARACIÓN RESPONSIVE - ¡EDITA AQUÍ!
      const SEPARACION = {
        mobile: 70, // < 768px - Muy juntas
        md: 130, // >= 768px - Moderadas
        lg: 170, // >= 1024px - Separadas
        xl: 190, // >= 1280px - Muy separadas
      };

      // Función para obtener la separación según el tamaño de pantalla
      const getSeparacion = () => {
        const width = window.innerWidth;
        if (width >= 1280) return SEPARACION.xl;
        if (width >= 1024) return SEPARACION.lg;
        if (width >= 768) return SEPARACION.md;
        return SEPARACION.mobile;
      };

      let separacionActual = getSeparacion();

      // 🎯 ESTADO INICIAL - Todo oculto
      gsap.set("#paloma-izq", { opacity: 0, x: -200 });
      gsap.set("#paloma-der", { opacity: 0, x: 200 });
      gsap.set("#hoja-fecha-izq", { opacity: 0 }); // Solo opacidad, SIN x
      gsap.set("#hoja-fecha-der", { opacity: 0 }); // Solo opacidad, SIN x
      gsap.set("#texto-invitacion", { opacity: 0, scale: 0.9 });
      gsap.set("#anio-evento", { opacity: 0, scale: 0.9 });
      gsap.set("#fecha-evento", { opacity: 0, scale: 0.9 });
      gsap.set("#hora-evento", { opacity: 0, scale: 0.9 });

      // ✨ ANIMACIÓN: PALOMAS VUELAN CON SEPARACIÓN RESPONSIVE
      ScrollTrigger.create({
        trigger: "#palomas-container",
        start: "top 85%",
        end: "top 20%",

        onEnter: () => {
          separacionActual = getSeparacion(); // Actualizar separación
          const tl = gsap.timeline();

          // 1. PALOMAS VUELAN DESDE LOS COSTADOS
          tl.to(
            "#paloma-izq, #paloma-der",
            {
              opacity: 1,
              duration: 1.5,
              ease: "power2.out",
            },
            0,
          )

            .to(
              "#paloma-izq",
              {
                x: -separacionActual, // Usa el valor responsive
                duration: 2.5,
                ease: "power2.out",
              },
              0,
            )

            .to(
              "#paloma-der",
              {
                x: separacionActual, // Usa el valor responsive
                duration: 2.5,
                ease: "power2.out",
              },
              0,
            )

            // 2. TEXTOS APARECEN
            .to(
              "#texto-invitacion, #anio-evento, #fecha-evento, #hora-evento",
              {
                opacity: 1,
                scale: 1,
                duration: 1,
                ease: "back.out(1.2)",
              },
              0.8,
            )

            // 3. HOJAS APARECEN - SOLO FADE IN (sin movimiento)
            .to(
              "#hoja-fecha-izq, #hoja-fecha-der",
              {
                opacity: 0.8, // Opacidad 80% como en RecepcionSocial
                duration: 1,
                ease: "power2.out",
              },
              1.3,
            );
        },

        onLeaveBack: () => {
          // Resetear todo al salir por arriba
          const tl = gsap.timeline();

          tl.to(
            "#paloma-izq, #paloma-der, #hoja-fecha-izq, #hoja-fecha-der, #texto-invitacion, #anio-evento, #fecha-evento, #hora-evento",
            {
              opacity: 0,
              duration: 0.5,
              ease: "power2.in",
            },
          )
            .to(
              "#paloma-izq",
              {
                x: -200,
                duration: 0.5,
                ease: "power2.in",
              },
              "<",
            )
            .to(
              "#paloma-der",
              {
                x: 200,
                duration: 0.5,
                ease: "power2.in",
              },
              "<",
            )
            .to(
              "#texto-invitacion, #anio-evento, #fecha-evento, #hora-evento",
              {
                scale: 0.9,
                duration: 0.5,
                ease: "power2.in",
              },
              "<",
            );
        },
      });

      // 📱 ACTUALIZAR SEPARACIÓN AL CAMBIAR TAMAÑO DE VENTANA
      let resizeTimeout;
      window.addEventListener("resize", () => {
        clearTimeout(resizeTimeout);
        resizeTimeout = setTimeout(() => {
          const nuevaSeparacion = getSeparacion();
          if (nuevaSeparacion !== separacionActual) {
            separacionActual = nuevaSeparacion;
            // Actualizar posición de palomas si ya están visibles
            gsap.to("#paloma-izq", { x: -separacionActual, duration: 0.3 });
            gsap.to("#paloma-der", { x: separacionActual, duration: 0.3 });
          }
        }, 150);
      });

      console.log("✨ Animaciones de fecha con separación responsive listas!");
    });
  }
</script>
